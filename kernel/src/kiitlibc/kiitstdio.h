#include "../limine.h"
void color_fbuff(struct limine_framebuffer* framebuffer, uint32_t color);
void k_putchar(unsigned short int character, int x, int y, uint32_t fg, uint32_t bg, struct limine_framebuffer* framebuffer); 
